<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
        force="true" on every entry below: without it, PHPUnit only applies
        an <env> value when the variable ISN'T already set in the real
        process environment. That's fine on a bare host shell, but inside
        the Docker `app`/`dev` container, docker-compose.yml's
        `env_file: .env` pre-sets every one of these same names (APP_ENV,
        DB_CONNECTION, etc.) before PHP even starts — so without `force`,
        every value here silently no-ops and the suite runs against
        whatever `.env` says (Postgres, database cache/session/queue) instead
        of this fast in-memory config. Confirmed: this manifested as every
        POST-based test failing with a 419 CSRF error, because APP_ENV
        stayed "local" instead of "testing" (Laravel's CSRF middleware only
        exempts console test runs when app()->environment() is "testing").
        -->
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file" force="true"/>
        <env name="BCRYPT_ROUNDS" value="4" force="true"/>
        <env name="BROADCAST_CONNECTION" value="null" force="true"/>
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="sqlite" force="true"/>
        <env name="DB_DATABASE" value=":memory:" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
        <env name="PULSE_ENABLED" value="false" force="true"/>
        <env name="TELESCOPE_ENABLED" value="false" force="true"/>
        <env name="NIGHTWATCH_ENABLED" value="false" force="true"/>
    </php>
</phpunit>
